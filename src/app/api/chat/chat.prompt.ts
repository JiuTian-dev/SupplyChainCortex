/**
 * System prompt for the SupplyChain Cortex chat assistant.
 *
 * Re-exported from lib/agent/prompts/system-prompt.ts for backward
 * compatibility. New imports should target lib/agent/prompts/system-prompt
 * directly so that lib modules do not reverse-depend on app/api.
 */
export { SYSTEM_PROMPT } from '@/lib/agent/prompts/system-prompt';
