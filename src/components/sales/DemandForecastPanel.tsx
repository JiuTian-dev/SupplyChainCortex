/**
 * DemandForecastPanel — REMOVED
 *
 * This component has been superseded by SalesForecastEnhanced which provides
 * a more comprehensive sales forecast with a broader data scope (30-day historical
 * + 14-day forecast vs. per-SKU exponential smoothing).
 *
 * If per-SKU demand forecasting is required in the future, re-implement using
 * SalesForecastEnhanced as a base rather than restoring this file.
 */
export {};
